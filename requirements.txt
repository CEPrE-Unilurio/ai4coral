requests
opencv-contrib-python
absl-py
bottle
gunicorn
Pillow
tensorflow 

# We dont need specify the version os packges, the pip wil install the lastes version
# When we species the version the pip run in endless loop of dependecy

# requests==2.18.4
# opencv-contrib-python==4.4.0.42
# absl-py==0.11.0
# bottle==0.12.19
# gunicorn==20.0.4
# Pillow==8.0.1
# tensorflow==2.4.0 